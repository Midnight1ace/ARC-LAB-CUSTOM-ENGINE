# Experiments

Saved experiment definitions live here. `DefaultExperiment.json` mirrors the first built-in ArcLab demo: Magnetic Particle Confinement.

The next implementation step is to add JSON loading so the executable can run an experiment directly from this folder.
