# Notes

- The current editor viewport uses a Win32 window and CPU software framebuffer.
- The software viewport is a bridge toward OpenGL or Vulkan, not the final renderer.
- Experiment JSON is intentionally simple and local-first.
- CSV and PPM outputs are generated into `ArcLab/results/`.
