# Plan

1. Keep the project building after each subsystem pass.
2. Finish the dependency-free simulation foundation.
3. Use the software editor viewport until the renderer data path is stable.
4. Add OpenGL/Vulkan rendering once particles, debug draw, and scene state are proven.
5. Bring in Dear ImGui and ImPlot after native app flow is stable.
