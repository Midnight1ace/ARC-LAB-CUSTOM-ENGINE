# Third Party

ArcLab currently builds without vendored third-party libraries.

Expected future dependencies:

- SDL3 or GLFW for cross-platform windows/input.
- OpenGL loader or Vulkan SDK integration.
- Dear ImGui and ImPlot for editor UI and graphs.
- Optional JSON library if the built-in parser becomes too small.
